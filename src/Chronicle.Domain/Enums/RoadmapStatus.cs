namespace Chronicle.Domain.Enums;

/// <summary>State of a future goal rendered below the Timeline's "today" marker.</summary>
public enum RoadmapStatus
{
    Planned = 0,
    InProgress = 1,
    Done = 2
}
