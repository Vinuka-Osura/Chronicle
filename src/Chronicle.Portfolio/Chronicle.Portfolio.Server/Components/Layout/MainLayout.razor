@inherits LayoutComponentBase

<div class="admin-shell">
    <header class="admin-bar">
        <a class="admin-brand" href="/admin">
            <span class="admin-brand-mark">CHRONICLE</span>
            <span class="admin-brand-sub">content admin</span>
        </a>

        @*
            Grouped by what the operator is doing rather than by entity: the work,
            then the person, then the timeline. Ten flat links would be a wall.
        *@
        <nav class="admin-nav">
            <NavLink href="/admin" Match="NavLinkMatch.All">Dashboard</NavLink>

            <span class="admin-nav-group">
                <NavLink href="/admin/projects">Projects</NavLink>
                <NavLink href="/admin/posts">Articles</NavLink>
            </span>

            <span class="admin-nav-group">
                <NavLink href="/admin/profile">Profile</NavLink>
                <NavLink href="/admin/experience">Experience</NavLink>
                <NavLink href="/admin/skills">Skills</NavLink>
                <NavLink href="/admin/certifications">Certifications</NavLink>
            </span>

            <span class="admin-nav-group">
                <NavLink href="/admin/chronology">Timeline</NavLink>
                <NavLink href="/admin/learning">Learning</NavLink>
                <NavLink href="/admin/roadmap">Roadmap</NavLink>
                <NavLink href="/admin/status">Status</NavLink>
            </span>

            <a href="/scalar/v1" target="_blank" rel="noreferrer">API</a>
        </nav>

        <AuthorizeView>
            <Authorized>
                <form class="admin-signout" action="/admin/logout" method="post">
                    <AntiforgeryToken />
                    <span class="admin-user">@context.User.Identity?.Name</span>
                    <button type="submit">Sign out</button>
                </form>
            </Authorized>
        </AuthorizeView>
    </header>

    <main class="admin-main">
        @Body
    </main>
</div>

<div id="blazor-error-ui" data-nosnippet>
    An unhandled error has occurred.
    <a href="." class="reload">Reload</a>
    <span class="dismiss">&#128473;</span>
</div>
